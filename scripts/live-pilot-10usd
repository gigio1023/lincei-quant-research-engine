#!/usr/bin/env bash
# Requires --confirm-real-money and preflight.ready; max $10 notional per V1 policy.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT/backend"
npm run v1:cli -- live-pilot-10usd "$@"
