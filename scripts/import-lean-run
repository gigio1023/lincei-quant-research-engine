#!/usr/bin/env bash
# Idempotent import of LEAN artifacts into Nest (arg: run id or "latest").
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
TARGET="${1:-latest}"
if (($# > 0)); then
  shift
fi
cd "$ROOT/backend"
bun run v1:cli -- import-lean-run "$TARGET" "$@"
