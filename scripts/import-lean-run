#!/usr/bin/env bash
# Idempotent import of LEAN artifacts into Nest (arg: run id or "latest").
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
TARGET="${1:-latest}"
cd "$ROOT/backend"
npm run v1:cli -- import-lean-run "$TARGET"
