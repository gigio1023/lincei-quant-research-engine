#!/usr/bin/env bash
# Runs LEAN backtest or local simulator fallback; writes artifacts/lean-runs/<runId>/.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
PROJECT="${1:-aggressive_llm_momentum}"
cd "$ROOT/backend"
bun run v1:cli -- lean-backtest "$PROJECT"
