# 서버 설정
PORT=3001
FRONTEND_URL=http://localhost:3000

# 데이터베이스
DATABASE_PATH=data/investment.db
# 개발 기본값은 synchronize=true입니다. 운영/실거래 준비 환경에서는
# TYPEORM_SYNCHRONIZE=false, TYPEORM_MIGRATIONS_RUN=true로 바꾸고
# 배포 전 npm run migration:show / npm run migration:run을 확인하세요.
TYPEORM_SYNCHRONIZE=true
TYPEORM_MIGRATIONS_RUN=false

# OpenAI API (보조 모델, 선택사항)
OPENAI_API_KEY=your_openai_api_key_here

# Gemini API (주 모델, 필수 - Google AI Studio에서 발급)
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# 추가 리포트 생성 옵션 (true/false)
ENABLE_MIDDAY_REPORT=false          # 정오 12시 추가 리포트
ENABLE_WEEKLY_REPORT=false          # 주간 전망 리포트 (일요일)
ENABLE_PERIODIC_NEWS_COLLECTION=false  # 2시간마다 뉴스 수집

# 알림 설정 (선택사항)
NOTIFICATION_WEBHOOK_URL=           # 리포트 생성 완료 시 웹훅 알림

# 로그 레벨
LOG_LEVEL=info
